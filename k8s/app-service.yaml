apiVersion: v1
kind: Service
metadata:
  name: devproject-app-service
  namespace: devproject
  labels:
    app: devproject-app
spec:
  selector:
    app: devproject-app
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 3000
      nodePort: 30080
